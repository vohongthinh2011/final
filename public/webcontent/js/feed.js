/*
DOM
document.querySelector('h1')
    -traverses depth first from the start of the document to find first instance
    

attributes method:
    .textContent //text inside the headers
    .innerHTML  // all content inside the headers
    .setAttribute('attribute', 'attribute value')
    .getAttribtue('attribute') //returns value of attribute
    

event handling methods
    .onclick = 'commands/functions' 
    
Classes/apis:
localStorage //api for storing browser data
    .setItem('name', 'value');  //stores value by name
    .getItem('name');  //retrieves data by name


output methods:
document.write('content');
alert('content');
prompt('content'); //requests an input from user and stores it


*/
